#! /usr/bin/env bash
# Uninstall with yaourt -Rns

rm -f $HOME/installed $HOME/unneeded $HOME/unneeded-opt

yaourt -Qqe  > $HOME/installed
yaourt -Qdtq > $HOME/unneeded
yaourt -Qdttq > $HOME/unneeded-opt


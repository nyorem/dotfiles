#! /usr/bin/env bash
# Uninstall with yaourt -Rcs

rm -f $HOME/installed $HOME/not-needed $HOME/not-needed-opt

yaourt -Qqe  > $HOME/installed
yaourt -Qdtq > $HOME/not-needed
yaourt -Qdttq > $HOME/not-needed-opt

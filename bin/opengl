#! /usr/bin/env bash
# Creating a sample OpenGL project
# $1 = name of the project

if [ -z $1 ]; then
	PROJECT=gl
else
	PROJECT=$1
fi

if [ -d $PROJECT ]; then
	echo "$PROJECT directory already exists !"
	exit 0
fi

cp -r $HOME/bin/gl $PROJECT

